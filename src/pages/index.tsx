import Image from 'next/image';
import { Inter } from 'next/font/google';
import { SideBar, TopBar } from '@src/components';

const inter = Inter({ subsets: ['latin'] });

export default function Home() {
  return (
    <div className="h-[100%] flex main">
    </div>
  );
}
